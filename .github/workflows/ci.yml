name: CI

on:
  push:
    branches: [main]
    paths-ignore:
      - '*.md'
      - 'docs/**'
      - 'README.md'
      - 'AGENTS.md'
  pull_request:
    branches: [main]
    paths-ignore:
      - '*.md'
      - 'docs/**'
      - 'README.md'
      - 'AGENTS.md'

jobs:
  test:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pull-requests: write
    env:
      FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: 'true'
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Run tests
        run: npm test -- --no-watch

      - name: Run lint
        run: npm run lint

      - name: Build
        run: npm run build

      - name: Upload build artifact
        uses: actions/upload-artifact@v4
        with:
          name: dist
          path: dist/proyecto-front/browser/
          retention-days: 90

      - name: Comment on PR
        if: github.event_name == 'pull_request'
        uses: actions/github-script@v7
        env:
          JOB_STATUS: ${{ job.status }}
          SHA: ${{ github.sha }}
        with:
          script: |
            const { eventName, payload, repository } = context;
            const pr = payload.pull_request;
            const conclusion = process.env.JOB_STATUS;
            const sha = process.env.SHA;

            const icon = conclusion === 'success' ? '✅' : '❌';
            const status = conclusion === 'success' ? 'passed' : 'failed';

            const body = `${icon} **CI ${status}** for commit ${sha.slice(0, 7)}\n\n` +
              '- Tests: passed\n' +
              '- Lint: passed\n' +
              '- Build: passed\n\n' +
              '> Build artifact available for download';

            pr && github.rest.issues.createComment({
              owner: repository.owner.login,
              repo: repository.repo,
              issue_number: pr.number,
              body: body
            });
